# include "main.h"

/**
 * _islower - checks for lowercase character
 * @b: the character to check
 * Return: 1 if b is lower, otherwise 0 
 */
int _islower(int b)
{
	return (b >= 'a' && b <= 'z');
}
